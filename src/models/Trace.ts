export class Trace {
  province: string;
  city: string;
  county: string;
  constructor(province: string, city: string, county: string) {
    this.province = province;
    this.city = city;
    this.county = county;
  }
}
